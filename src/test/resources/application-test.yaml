spring.cache.type: none
spring:
  init:
    mode: always
  datasource :
    url : jdbc:h2:mem:test
    username : sa
    password :
    driver-class-name : org.h2.Driver
  jpa :
    hibernate :
      ddl-auto : update
  liquibase:
    change-log : db/changelog-h2.sql
  flyway :
    enabled : false
  sql :
    init :
      schema-locations : classpath:db/changelog-h2.sql
      data-locations : classpath:db/data-h2.sql